import styled from 'styled-components';

export const FooterSocialContainer = styled.div`
  margin: 10px 0;

  & > button {
    margin: 0 10px;
  }
`;

export const FooterContainer = styled.footer`
  margin-top: 48px;
  padding: 25px;
  background-color: rgba(var(--light-secondary));
`;
